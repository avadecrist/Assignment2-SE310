package main.java.com.se310.store.factory;



// Created this class, it's a public enum to handle different product types for Product Factory
// Since it wasn't defined originally, this seemed best to meet specifications
// without modifying the model classes where it wasn't needed


public enum ProductType {
    STANDARD,
    DISCOUNTED,
    PREMIUM
}
